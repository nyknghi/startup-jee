<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE struts-config PUBLIC "-//Apache Software Foundation//DTD Struts Configuration 1.2//EN"
                               "http://jakarta.apache.org/struts/dtds/struts-config_1_2.dtd">
<struts-config>
 <data-sources/>
 <form-beans>
  <form-bean name="LoginForm" type="com.vaannila.LoginForm"/>
  <form-bean name="creerStartupForm" type="com.vaannila.creerStartupForm"/>
  <form-bean name="OrganiserLeveeForm" type="com.vaannila.OrganiserLeveeForm"/>
  <form-bean name="CreerInvestisseurForm" type="com.vaannila.CreerInvestisseurForm"/>
  <form-bean name="ParticiperLeveeForm" type="com.vaannila.ParticiperLeveeForm"></form-bean>
 </form-beans>
 <global-exceptions/>
 <global-forwards>
  <forward name="welcome" path="/Welcome.do"/>
 </global-forwards>
 <action-mappings>
  <action input="/login.jsp" name="LoginForm" path="/Login"
   scope="session" type="com.vaannila.LoginAction">
   <forward name="success" path="/admin.jsp"/>
   <forward name="failure" path="/failure.jsp"/>
   <forward name="fondateur" path="/fondateur.jsp"/>
   <forward name="ba" path="/BA.jsp"/>
  </action>
  <action input="/creerstartup.jsp" name="creerStartupForm" path="/CreerStartup"
   scope="session" type="com.vaannila.creerStartupAction">
  </action>
  <action input="/OrganiserLevee.jsp" name="OrganiserLeveeForm" path="/OrganiserLevee"
   scope="session" type="com.vaannila.OrganiserLeveeAction">
  </action>  
  <action input="/ParticiperLevee.jsp" name="ParticiperLeveeForm" path="/ParticiperLevee"
   scope="session" type="com.vaannila.ParticiperLeveeAction">
  </action>   
  <action input="/CreerInvestisseur.jsp" name="CreerInvestisseurForm" path="/CreerInvestisseur"
   scope="session" type="com.vaannila.CreerInvestisseurAction">
  </action>    
  <action forward="/welcomeStruts.jsp" path="/Welcome"/>
  <action name="creerStartupForm" path="/creerstartup"/>
 </action-mappings>
 <controller processorClass="org.apache.struts.tiles.TilesRequestProcessor"/>
 <message-resources parameter="com/vaannila/ApplicationResource"/>
 <!-- ========================= Tiles plugin ===============================

    This plugin initialize Tiles definition factory. This later can takes some
    parameters explained here after. The plugin first read parameters from
    web.xml, thenoverload them with parameters defined here. All parameters
    are optional.
    The plugin should be declared in each struts-config file.
    - definitions-config: (optional)
    Specify configuration file names. There can be several comma
    separated file names (default: ?? )
    - moduleAware: (optional - struts1.1)
    Specify if the Tiles definition factory is module aware. If true
    (default), there will be one factory for each Struts module.
    If false, there will be one common factory for all module. In this
    later case, it is still needed to declare one plugin per module.
    The factory will be initialized with parameters found in the first
    initialized plugin (generally the one associated with the default
    module).
    true : One factory per module. (default)
    false : one single shared factory for all modules
    - definitions-parser-validate: (optional)
    Specify if xml parser should validate the Tiles configuration file.
    true : validate. DTD should be specified in file header (default)
    false : no validation

    Paths found in Tiles definitions are relative to the main context.
    -->
 <plug-in className="org.apache.struts.tiles.TilesPlugin">
  <set-property property="definitions-config" value="/WEB-INF/tiles-defs.xml"/>
  <set-property property="moduleAware" value="true"/>
 </plug-in>
 <!-- ========================= Validator plugin ================================= -->
 <plug-in className="org.apache.struts.validator.ValidatorPlugIn">
  <set-property property="pathnames" value="/WEB-INF/validator-rules.xml,/WEB-INF/validation.xml"/>
 </plug-in>
</struts-config>
